{
  "name": "print-agent",
  "productName": "Print Agent",
  "version": "0.1.0",
  "description": "Local print agent ZPL loopback untuk dsmart (Zebra ZD220)",
  "author": "dsmart",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron . --no-sandbox",
    "dist": "electron-builder --win --x64 && node scripts/pack-installer-zip.js",
    "dist:exe": "electron-builder --win --x64",
    "release": "electron-builder --win --x64 --publish always",
    "release:auto": "node scripts/release.js",
    "zip": "node scripts/pack-installer-zip.js",
    "pack": "electron-builder --dir"
  },
  "dependencies": {
    "electron-store": "^8.2.0",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "archiver": "^5.3.2",
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "app.dsmart.print-agent",
    "productName": "Print Agent",
    "copyright": "Copyright © 2026",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "src/**/*",
      "!src/**/*.bak",
      "assets/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "src/main/printer/*.ps1"
    ],
    "extraResources": [
      {
        "from": ".env.example",
        "to": ".env.example"
      }
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "ngabroger",
        "repo": "agent-print"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "PrintAgent-Setup-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": false,
      "createStartMenuShortcut": true,
      "shortcutName": "Print Agent",
      "runAfterFinish": true
    }
  }
}
